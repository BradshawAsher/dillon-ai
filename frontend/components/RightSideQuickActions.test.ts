import { describe, expect, it } from 'vitest'
import { RightSideQuickActions } from './RightSideQuickActions'

describe('RightSideQuickActions', () => {
    it('is defined and is a valid React component function', () => {
        expect(RightSideQuickActions).toBeDefined()
        expect(typeof RightSideQuickActions).toBe('function')
    })
})
